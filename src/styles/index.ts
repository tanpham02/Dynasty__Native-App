import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    shadowX: {
        shadowOffset: {
            height: 5,
            width: 5
        },
        shadowRadius: 3,
        shadowOpacity: 0.8,
        shadowColor: "#c3c3c3",
        elevation: 10
    },
})

export default styles